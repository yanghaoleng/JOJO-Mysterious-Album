import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-40-1","category":"食物","name":"小圆南瓜","family":"pumpkin","variant":1,"seed":40}); }
