import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-52-0","category":"食物","name":"海苔卷寿司","family":"sushi","variant":0,"seed":52}); }
