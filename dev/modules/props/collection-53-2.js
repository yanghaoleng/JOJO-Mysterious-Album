import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-53-2","category":"食物","name":"宽面面条","family":"bowl","variant":2,"seed":53}); }
