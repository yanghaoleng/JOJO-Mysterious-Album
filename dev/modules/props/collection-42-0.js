import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-42-0","category":"食物","name":"大圆番茄","family":"fruit","variant":0,"seed":42}); }
