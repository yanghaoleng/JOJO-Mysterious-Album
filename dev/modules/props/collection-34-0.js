import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-34-0","category":"食物","name":"大个菠萝","family":"pineapple","variant":0,"seed":34}); }
