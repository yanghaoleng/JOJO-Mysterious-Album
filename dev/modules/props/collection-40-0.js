import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-40-0","category":"食物","name":"大圆南瓜","family":"pumpkin","variant":0,"seed":40}); }
