import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-5-0","category":"交通工具","name":"客车","family":"bus","variant":0,"seed":5}); }
