import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-75-0","category":"玩具","name":"摇铃","family":"rattle","variant":0,"seed":75}); }
