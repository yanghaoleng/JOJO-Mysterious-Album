import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-53-4","category":"食物","name":"蔬菜面面条","family":"bowl","variant":4,"seed":53}); }
