import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-45-0","category":"食物","name":"单个马卡龙","family":"macaron","variant":0,"seed":45}); }
