import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-11-0","category":"交通工具","name":"挖掘机","family":"digger","variant":0,"seed":11}); }
