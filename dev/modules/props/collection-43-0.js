import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-43-0","category":"食物","name":"圆形饼干","family":"cookie","variant":0,"seed":43}); }
