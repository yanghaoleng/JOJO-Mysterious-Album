import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-55-3","category":"食物","name":"可可","family":"cup","variant":3,"seed":55}); }
