import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-73-0","category":"玩具","name":"魔方","family":"cube","variant":0,"seed":73}); }
