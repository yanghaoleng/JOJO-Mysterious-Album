import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-50-4","category":"食物","name":"拼盘饺子","family":"dumpling","variant":4,"seed":50}); }
