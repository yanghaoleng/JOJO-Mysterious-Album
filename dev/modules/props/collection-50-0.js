import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-50-0","category":"食物","name":"单只饺子","family":"dumpling","variant":0,"seed":50}); }
