import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-50-1","category":"食物","name":"双只饺子","family":"dumpling","variant":1,"seed":50}); }
