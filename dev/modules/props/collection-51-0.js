import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-51-0","category":"食物","name":"圆包包子","family":"bun","variant":0,"seed":51}); }
