import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-6-0","category":"交通工具","name":"面包车","family":"van","variant":0,"seed":6}); }
