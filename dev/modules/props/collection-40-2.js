import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-40-2","category":"食物","name":"扁圆南瓜","family":"pumpkin","variant":2,"seed":40}); }
