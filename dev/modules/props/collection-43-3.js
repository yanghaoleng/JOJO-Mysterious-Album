import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-43-3","category":"食物","name":"夹心饼干","family":"cookie","variant":3,"seed":43}); }
