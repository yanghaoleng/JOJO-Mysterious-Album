import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-27-0","category":"交通工具","name":"雪橇","family":"sled","variant":0,"seed":27}); }
