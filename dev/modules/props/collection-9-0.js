import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-9-0","category":"交通工具","name":"越野车","family":"car","variant":0,"seed":9}); }
