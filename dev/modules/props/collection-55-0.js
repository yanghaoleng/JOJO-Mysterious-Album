import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-55-0","category":"食物","name":"牛奶","family":"cup","variant":0,"seed":55}); }
