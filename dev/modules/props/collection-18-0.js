import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-18-0","category":"交通工具","name":"高铁","family":"train","variant":0,"seed":18}); }
