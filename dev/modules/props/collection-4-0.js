import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-4-0","category":"交通工具","name":"校车","family":"bus","variant":0,"seed":4}); }
