import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-12-0","category":"交通工具","name":"装载机","family":"loader","variant":0,"seed":12}); }
