import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-28-4","category":"食物","name":"对半梨","family":"fruit","variant":4,"seed":28}); }
