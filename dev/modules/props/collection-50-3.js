import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-50-3","category":"食物","name":"蒸笼饺子","family":"dumpling","variant":3,"seed":50}); }
