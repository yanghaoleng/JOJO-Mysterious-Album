import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-51-4","category":"食物","name":"拼盘包子","family":"bun","variant":4,"seed":51}); }
