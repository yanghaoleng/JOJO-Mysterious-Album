import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-15-0","category":"交通工具","name":"罐车","family":"tanker","variant":0,"seed":15}); }
