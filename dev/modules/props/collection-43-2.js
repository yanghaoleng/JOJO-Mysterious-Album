import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-43-2","category":"食物","name":"星形饼干","family":"cookie","variant":2,"seed":43}); }
