import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-1-0","category":"交通工具","name":"警车","family":"car","variant":0,"seed":1}); }
