import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-50-2","category":"食物","name":"三只饺子","family":"dumpling","variant":2,"seed":50}); }
