import { buildCollection } from '../collection-model.js';
export function build(toolkit) { buildCollection(toolkit, {"id":"collection-13-0","category":"交通工具","name":"起重车","family":"crane","variant":0,"seed":13}); }
