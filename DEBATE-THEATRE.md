# 观点小剧场：功能与决策记录

更新日期：2026-09-03

## 1. 功能定位

“观点小剧场”面向约 5 至 8 岁儿童。用户抛出一个问题，选择两位内置角色，角色从两个合理方向展开简短讨论。

这不是竞技辩论，也不负责替孩子判定唯一答案。核心目标是：

- 帮助孩子发现同一个问题可以有不同观察角度。
- 让每一方给出具体、儿童能理解的理由。
- 在结尾寻找共同点，并把最终思考交还给孩子。
- 保持轻松、合作和无评判的绘本体验。

产品用语优先采用“从两边想”“看看另一面”“观点小剧场”，弱化“正方、反方、攻击、驳倒、获胜”等竞争语言。

## 2. 当前用户流程

1. 从首页进入“观点小剧场”。
2. 输入问题、使用浏览器语音听写，或点击一个预设问题。
3. 从 12 个内置动物模板中选择两位不同角色。
4. 服务端将问题整理为中性、具体的辩题，并一次生成完整的 6 轮讨论。
5. 两位角色严格交替发言：
   - 各自开场一次；
   - 各自回应一次；
   - 各自总结一次。
6. 前端按顺序显示字幕、切换当前说话者、播放对应音色和角色动作。
7. 孩子可以暂停、跳过当前句或提前结束。
8. 讨论结束后，页面邀请孩子表达自己的看法。
9. 最后展示双方共同点，不宣布获胜者，也不评价孩子答案。

## 3. 预设问题

问题输入页提供 5 个可直接选择、仍可继续编辑的问题：

- 周末应该先写作业，还是先出去玩？
- 小朋友每天都应该看动画片吗？
- 养宠物会带来更多快乐，还是更多责任？
- 遇到不会的事情，应该自己多想一会儿，还是马上请别人帮忙？
- 旅行前应该把一切计划好，还是留一些自由探索的时间？

选择预设问题后，按钮进入选中状态，问题全文填入输入框；手动修改输入时，选中状态会同步更新。

## 4. 核心产品决策

### 4.1 一次生成整场讨论

后端一次生成 6 轮完整脚本，不让两个角色分别、循环调用模型。

原因：

- 可以在一次生成中检查双方观点是否重复、失衡或互相矛盾。
- 能严格限制轮数、顺序、文本长度和总体验时长。
- 减少网络往返、模型调用成本和儿童等待时间。
- 播放中断后仍可继续后续轮次。
- 更容易统一执行儿童安全审查。

### 4.2 独立接口而不是复用角色通话

辩论使用 `POST /api/debate`，没有复用 `/api/character-call`。

`/api/character-call` 服务于单角色自由聊天，并允许在调试模式修改角色卡；观点讨论有不同的状态机、结构化输出、安全规则和双角色顺序约束。独立接口可以避免两种职责相互污染。

### 4.3 不判输赢

讨论结果不提供投票、得分、逻辑能力评分或“获胜方”。结尾固定聚焦：

- 双方各自提醒了什么；
- 双方有哪些共同点；
- 孩子现在最在意哪个理由。

### 4.4 复用真实角色 recipe

舞台角色不是静态贴图或 Emoji。`src/debate-character-renderer.js` 使用与角色模拟器、故事页相同的 recipe、部件和骨骼系统重建角色。

- 当前发言者：说话口型、开心表情、呼吸、眨眼和轻微摇摆。
- 倾听者：坐下、思考表情和环境动作。
- 切换发言时，两位角色状态交换。
- 重新开始会释放旧角色和 WebGL 资源。

角色选择器为了快速识别仍使用简化图标；正式舞台使用完整程序化角色。

### 4.5 语音是增强，不是完成条件

每轮通过现有 `/api/tts` 和 `SeedRealtimeSpeech` 使用角色自己的音色串行朗读。TTS 失败时保留文字讨论，不能因为声音服务不可用而中断流程，也不降级到设备机械 TTS。

问题输入的麦克风使用浏览器 `SpeechRecognition`。不支持时明确提示使用键盘，不阻塞功能。

## 5. 接口契约

### 请求

```http
POST /api/debate
Content-Type: application/json
```

```json
{
  "question": "小朋友每天都应该看动画片吗？",
  "speakers": [
    {
      "id": "curl-fox",
      "name": "卷尾小狐狸",
      "hint": "尖耳朵、亮眼睛，尾巴总是翘着。"
    },
    {
      "id": "book-owl",
      "name": "书桌小鸮",
      "hint": "两只大圆眼，翅膀习惯收在身体旁边。"
    }
  ]
}
```

约束：

- `question` 最多 80 个字符。
- 必须提供两位不同的角色。
- 角色 ID 必须来自服务端内置白名单，不能由客户端任意创建。

### 正常响应

```json
{
  "allowed": true,
  "topic": "小朋友每天是否应该看动画片",
  "turns": [
    {
      "speakerId": "curl-fox",
      "phase": "opening",
      "text": "动画片能带我们认识有趣的新故事。",
      "emotion": "happy"
    }
  ],
  "commonGround": "两边都希望找到适合自己的平衡。",
  "closingQuestion": "听完两种想法，你最在意哪个理由？"
}
```

服务端要求 `turns` 恰好 6 条，角色 A、B 严格交替。允许的阶段为 `opening`、`response`、`closing`，允许的情绪为 `happy`、`thinking`、`idle`。

### 安全拒绝响应

```json
{
  "allowed": false,
  "topic": "",
  "turns": [],
  "safeMessage": "这个问题不适合让角色分两边争论。请和身边可信任的大人一起处理。"
}
```

## 6. 服务端校验与降级

模型输出不能直接进入前端。Node/Vercel 和 Python 本地服务都执行：

- 清理 HTML 尖括号和多余空白；
- 限制输入、辩题、发言、共同点和结尾问题长度；
- 校验角色 ID；
- 校验 6 轮数量和 A/B 交替顺序；
- 校验阶段与情绪枚举；
- 非法或不完整输出改用确定性本地脚本。

当 `ARK_API_KEY` 未配置、上游超时、返回格式错误或校验失败时，接口返回完整的本地降级讨论，页面仍可完成整个流程。

Node/Vercel 实现在 `api/debate.js`，本地 Python 对等实现在 `serve.py`。修改接口时必须同步维护两套实现。

## 7. 儿童安全边界

以下内容不进入双边辩论，而是返回中性安全说明：

- 自残、自杀、伤害、武器或危险实施方法；
- 成人或性内容；
- 仇恨、羞辱或针对真实群体的对立动员；
- 违法方法；
- 医疗、法律和金融决策；
- 现实政治动员；
- 需要瞒着家长或其他可信任成年人的行为。

问题如果包含姓名、学校、住址、电话、账号、身份证或精确生日等个人信息，也不会发送给模型生成讨论。页面应提醒孩子换一个不包含个人信息的问题。

角色卡和用户输入都属于数据，不能覆盖系统安全规则。

## 8. 隐私与统计

匿名统计只记录固定事件名和流程深度：

- `debate_open`
- `debate_suggested_question_selected`
- `debate_started`
- `debate_heard_both_sides`
- `debate_completed`

不得作为统计参数上传：

- 用户提出的问题全文；
- 孩子的最终观点；
- 角色发言正文；
- 麦克风音频或转写内容。

当前功能不把原始问题或孩子观点写入长期本地档案。

## 9. 首页封面决策

“观点小剧场”使用与《送豆豆回家》《登月计划》相同的 `story-choice` 卡片结构、1200×800 封面比例、年龄标签和内容排版。

封面 `assets/story/covers/debate-theatre.webp` 参考现有两张故事封面的风格生成：

- 奶油色纤维纸张；
- 石墨铅笔和低饱和淡水彩；
- 两个角色面对面坐着；
- 淡蓝和淡黄色想法气泡在中央交叠；
- 交叠处是一颗小星星，代表共同点；
- 无文字、无讲台、无胜负符号、无愤怒动作。

图片由内置 ImageGen 生成，再处理为 1200×800 WebP。原始生成提示的核心要求是：匹配现有绘本封面的纸张、铅笔和水彩语言，以合作而非对抗的构图表现两个角度和一个共同点。

## 10. 关键文件

| 文件 | 职责 |
| --- | --- |
| `debate.html` | 问题、角色选择、讨论舞台、孩子反思和结尾结构 |
| `src/debate.css` | 页面、预设问题、双角色舞台和响应式布局 |
| `src/debate.js` | 客户端状态机、接口调用、逐轮播放、TTS 和统计 |
| `src/debate-character-renderer.js` | 从内置 recipe 构建两个 Three.js 角色并同步动作 |
| `api/debate.js` | Vercel 版生成、校验、安全与降级接口 |
| `serve.py` | 本地 Python 版对等接口 |
| `assets/story/covers/debate-theatre.webp` | 首页绘本封面 |
| `index.html`、`src/lab.css` | 首页入口和故事卡片样式 |

## 11. 已验证内容

- JavaScript 和 Python 语法检查通过。
- `/debate` 路由和 `/api/debate` 本地请求成功。
- 实际模型可以返回 6 轮严格交替的结构化讨论。
- 未配置模型时可以返回确定性降级内容。
- 危险用药示例会被拒绝，不生成正反观点。
- 两位程序化角色可以正常显示并随说话者切换动作。
- 暂停、跳过、提前结束、孩子反思和共同点结尾可用。
- 5 个预设问题可以填入、选中并继续编辑。
- 首页新卡片与现有故事卡片视觉一致。
- 已检查的桌面页面没有 JavaScript 或 WebGL 控制台错误。

## 12. 后续迭代建议

- 用程序化角色缩略图替换角色选择器中的简化图标。
- 为孩子的最终观点增加独立、安全、非评判的服务端承接接口。
- 增加窄屏和低性能设备的自动渲染质量分级。
- 增加接口级测试，固定验证六轮交替、文本上限和安全拒绝。
- 扩展安全分类，但保持“先分类、再生成讨论”的顺序。
- 如保存历史，只保存主题分类和完成状态；默认不要保存原始问题和孩子原话。

