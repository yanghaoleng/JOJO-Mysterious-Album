# 任务交接

更新时间：2026-08-25

## 2026-08-25 腾讯云迁移与数据后台

- [x] 在腾讯轻量服务器 `lhins-qgi1l9jg / 124.221.104.244` 建立独立的 `/opt/kindergrimm` 版本目录、`kindergrimm.service`、Nginx 站点和持久化 SQLite 目录；不改动同机 Rive 服务。
- [x] 增加匿名统计：浏览器 UV、页面 UV/PV、只在可见且聚焦时累计的有效停留、最高交互深度和预设交互事件；不上传孩子输入、姓名、声音、原始 IP 或精确位置。
- [x] 增加 `/Data` 管理后台，复用罗师傅档期页的六位数字键盘体验；口令与会话签名只配置在服务器环境变量，不进入 Git。
- [x] 移除全局声音开关；Fish Audio 教学和反馈按剧情自动播放，同时保留文字反馈。
- [x] 首次入口改为按模式加载：入口压缩传输约 22KB，故事模式增量约 575KB，角色实验室增量约 707KB；音频继续按播放进度加载和缓存。
- [x] 资源盘点：完整静态文件约 24.93MB，其中语音 13.47MB、音乐 5.83MB、图片 1.07MB、代码和页面 4.32MB；完整发布压缩包约 21.48MB，用户不会一次下载全部资源。
- [ ] DNSPod 的 `jma.mikeywa.site` A 记录仍需从 `76.76.21.21` 切至 `124.221.104.244`，再签发腾讯机 HTTPS 证书；当前浏览器管理策略校验不可用，无法安全进入腾讯控制台。旧 Vercel 继续提供正式域名，腾讯机已可随时接管。

## 当前目标

以 GitHub 项目 `albertobeiz/kindergrimm` 为视觉和角色技术基础，完成“萌萌星的奇妙图鉴”的第一个新手引入、首个箱庭关卡，以及可独立调试角色塑形、声音和儿童问答的角色实验室。

## 已完成

- [x] 拉取并保留 Kindergrimm 的程序化水彩角色、骨骼和动画体系。
- [x] 完成从叫醒角色、选择能力、性格情境、取名到世界生成的新手引入。
- [x] 完成斜俯视 Three.js 箱庭“雾灯花园”。
- [x] 支持点击、触摸、方向键和 WASD 移动。
- [x] 完成 3 颗萤火种收集、雾灯点亮、雾门出现和能力验证。
- [x] 让预设能力和自由生成能力都在关卡末尾产生实际可见效果。
- [x] 世界导演优先使用火山方舟 `doubao-seed-2-0-mini-260428`，并保留本地确定性降级。
- [x] 使用 Fish Audio 儿童感音色预生成 10 段内置教学语音。
- [x] 完成成长记录、本机图鉴保存、默认 Fish Audio 播放和减弱动态支持；当前版本不提供声音开关。
- [x] 完成桌面、手机竖屏和手机横屏响应式布局。
- [x] 密钥放入被忽略的 `.env.local`，仓库只保留 `.env.example`。
- [x] 首次进入增加故事模式/角色实验室选择，并支持角落随时切换。
- [x] 完成捏脸、身体、四种内置音色、模拟对话和角色实时预览。
- [x] 完成 6 阶段 17 问儿童画像，覆盖基础年龄段、学习与专注、探索与受挫、表达与社交、兴趣玩法、情绪支持和体验敏感项，并支持旧档案续答。
- [x] 保证“擅长看/听”的核心外貌不会被后续兴趣题覆盖。
- [x] 新增 4 段角色音色试听，共 14 段本地 MP3，运行时不依赖 Fish Audio 网络。
- [x] 将星仔设为玩家默认音色，并迁移旧实验室档案到新的默认设置。
- [x] 新增 `/api/tts`，角色反馈和模拟台词优先使用 Fish Audio 免费模型。
- [x] 预制并扩充至 67 段星仔离线语音，项目内置音频总数增至 81 段。
- [x] 新增 22 个程序化水彩场景，覆盖日常、自然和奇遇三类。
- [x] 场景同步调整角色比例、落脚位置和环境动作；宇宙实现失重漂浮。
- [x] 新增 15 个完整角色模板，包括 12 个小动物和 3 个特色人物，全部使用真实 recipe 生成预览与角色。
- [x] 安装并接入 Calligraph，重做角色头顶气泡，加入每次换句入场、逐字显示和减弱动态降级。
- [x] 新增鼠标与触摸目光跟随，角色会注视底部按钮，离开预览后平滑回正。
- [x] 新增点击角色的 4 组文字、动作和星仔语音反馈，并限制连续误触。
- [x] 取消角色实验室的系统 TTS 降级，固定星仔台词优先播放 Fish Audio 缓存。
- [x] 取消故事模式遗留的系统 TTS 降级，10 段教学引导仅使用预制 Fish Audio。
- [x] 新增 19 段模板与点击反馈缓存，并重新生成“随机换脸”，项目内置音频总数增至 81 段。
- [x] 将气泡改为角色头顶实时锚定，尖角水平居中，场景缩放和漂浮时同步跟随。
- [x] 将挥手、5 种表情和新增连续动作统一收进“动作”标签，移除预览底部常驻测试按钮。
- [x] 将“随机换一只”简化为预览右下角的低强调度“换”字印章。
- [x] 新增森林、雨天、星星、礼物和睡前共 5 套连续互动脚本，每套 3 问并保留已选记录。
- [x] 脚本实现“选项保留、反馈先完成、下一题再出现”，完成后把偏好摘要写入本机兴趣档案。
- [x] 新增 61 段动作与脚本星仔缓存，星仔固定语音增至 128 段，项目内置 MP3 增至 142 段。
- [x] 将实验室与第一关角色部件分辨率从 88 提高到 176，WebGL 画布使用 1.5 至 2.5 倍像素比并优化纹理采样，保留手绘颗粒但消除放大贴图感。
- [x] 画像进度按题保存、刷新后从首个未答问题继续；右侧笔记改为 6 组摘要和 17 项完成度。
- [x] 第一关接入画像启发：主题/玩法影响开场和收集提示，受挫/鼓励偏好影响雾门提示，角色称号/故事感觉进入结局。
- [x] 为新增画像问题和反馈预制 27 段星仔 Fish Audio，星仔固定语音增至 155 段，项目内置 MP3 增至 169 段。
- [x] 已将本轮完整成果推送到 `yanghaoleng/kindergrimm` 的 `main`，并发布到 `jma` Production；正式域名和备用域名均已切换，旧部署保留为回滚点。

## 运行状态

- 本地入口：`python3 serve.py 8137`
- Vercel 生产项目：`jma`，正式入口 `https://jma.mikeywa.site`，备用入口 `https://jma-zeta.vercel.app`。
- 当前生产部署由 Vercel 项目 `jma` 的 Production 目标管理；自定义域名 DNS 与 HTTPS 已生效，旧部署保留为回滚点。
- 页面：`http://localhost:8137/`
- 健康检查：`GET /api/health`
- AI 接口：`POST /api/director`，服务端使用火山方舟 Doubao Seed 2.0 Mini。
- 本地密钥：`.env.local`，严禁提交。
- 运行时语音：默认星仔固定台词优先本地 Fish Audio 缓存，其他音色和动态台词使用 `/api/tts`；`assets/voice/` 共 169 段静态 MP3，不使用系统 TTS。

## 本轮验证

- 清晰度：角色部件绘制密度为 176；桌面和 390×844 手机的 WebGL 画布实测物理像素/显示像素均为 1.5，纹理和页面均无错误覆盖层。
- 画像桌面回归：首题显示 0/17，选择后保留高亮与反馈，刷新后从第 2/17 题继续，进度和右侧分组笔记同步为 1/17。
- 画像手机回归：390×844 下预览在上、答题在下，进度可见、笔记隐藏、页面水平溢出为 0。
- 关卡消费回归：写入“宇宙和科学 / 收集图鉴 / 点子发明家 / 小提示”画像后，雾灯花园开场实际出现星空与收集提示，雾门与结局启发映射通过。
- 离线语音：27 段新增画像问答缓存均生成成功；`assets/voice/star/` 共 155 段，项目 MP3 共 169 段。
- 本轮 GitHub `main` 与 Vercel `jma` Production 均已完成；正式页面、关键资源哈希、169 段语音健康统计、火山世界导演和 Fish Audio TTS 均已通过线上验收。

- 2026-08-25 模型配置：世界导演优先使用火山方舟 Doubao Seed 2.0 Mini；Seedream 5.0 Lite 4K 仅保留服务端环境变量预留；健康检查不会返回密钥。
- Fish Audio 免费模型存在明显延迟波动；Vercel 函数上限设为 90 秒，上游等待 75 秒，避免把正常生成提前判为失败。
- 生产 TTS 验收：同步最新 Fish Audio 密钥后，`POST /api/tts` 返回 `200 audio/mpeg`，32,599 字节，真实服务端链路可用。
- 生产健康检查目标：火山方舟与 Fish Audio 均配置，健康接口只返回布尔状态和模型名。
- DNS 核验：`mikeywa.site` 权威 NS 为 DNSPod（`light.dnspod.net`、`quiet.dnspod.net`），不是 Cloudflare；不能在 Cloudflare 添加一条不会生效的记录。

- 桌面 1440x900：自由能力路线从引导到通关完整实测，成长报告文案和本机保存成功。
- 手机竖屏 390x844：首屏和四项能力选择全部在视口内，无水平或页面级纵向溢出。
- 手机横屏 844x390：面板内部可滚动，自由想法输入和提交按钮可达，无页面级溢出。
- 火山方舟真实请求：使用 2026-08-25 最新提供的 ARK 密钥，返回能力机制、名称、叙述句和专属过门句。
- 首次模式选择 1440x900、390x844：两种入口清晰，无页面溢出。
- 角色实验室 1440x900：三栏完整显示，捏脸、身体、声音、对话和复制配方均可用。
- 问答塑形回归：选择“更会听”后继续选择“花草和森林”，仍保留豆豆眼和 2.15 倍大软耳朵，只叠加雀斑。
- 手机竖屏 390x844：预览在上、工具在下，模式按钮不遮挡答案，无水平或页面级纵向溢出。
- 手机横屏 844x390：预览左右分栏，工具区独立滚动，无页面级溢出。
- 故事模式回归：从实验室切回后欢迎页、声音开关和“轻轻叫醒它”均正常。
- 角色实验室默认音色：重新开始后声音页选中“星仔”。
- Fish Audio 在线：真实 `/api/tts` 请求返回 `audio/mpeg`，页面音频来源为 `fish`。
- Fish Audio 缓存：67 段星仔固定台词均为 MP3；默认星仔播放来源标记为 `cached-fish`。
- 场景册桌面 1440x900：22 个缩略场景全部渲染，分类、选中态和实时舞台切换正常。
- 星星宇宙：深色水彩星空、无地面角色、失重漂浮和场景语音均正常。
- 场景册手机 390x844：两列缩略图、分类和滚动可达，无页面级溢出；宇宙角色不被气泡遮挡。
- 角色模板桌面：15 张真实角色缩略图全部渲染，逐一套用成功；动物和人物筛选分别显示 12 张和 3 张。
- 角色模板可编辑性：套用“安静小画家”后修改为豆豆眼，模板选中态正确解除，兴趣档案不受影响。
- 气泡动效：换句后 350ms 可见前 8 个字，约 1.9s 完整显示 19 个字形；Calligraph 加载状态正常、无模块错误。
- 手机竖屏 390x844：模板保持两列、气泡宽 350px 且自然换行，页面水平溢出为 0。
- 故事模式回归：从模板页切回故事再返回角色实验室，Three.js 舞台、实验室状态和 Calligraph 均正常恢复。
- 目光模块回归：连续目标的左看、右看、向下看和松开回正均通过；底部按钮区域会映射为向下目光。
- Fish Audio 缓存：`action-random`、4 段点击反馈和 15 段角色模板音频均已生成；67 个星仔清单项无缺失、无短文件，81 个 MP3 格式检查全部通过。
- 机械音清理：故事模式与角色实验室源码均不再包含 `speechSynthesis` 或 `SpeechSynthesisUtterance`；固定台词走本地 Fish Audio，动态失败只保留视觉反馈。
- 健康检查：世界导演与 Fish Audio 均已配置，142 段内置语音可读取。
- 动作页桌面 1440x1000：6 个动作、5 个表情和 5 套脚本全部可达，角色预览底部不再出现挥手与换表情按钮。
- 气泡几何：尖角计算样式位于气泡宽度 50%，气泡坐标来自角色头顶投影；桌面与手机均保持在预览边界内。
- 连续脚本实测：完整走通“森林里的两条小路”三问，已选按钮保持高亮、历史持续显示，反馈语音结束后才切换下一题，最终摘要成功写入“互动观察”。
- Fish Audio 缓存：连续脚本、动作和表情均标记为 `cached-fish`；128 个星仔清单项无缺失或短文件，142 个 MP3 格式检查全部通过。
- 手机竖屏 390x844：气泡、角色、随机印章和两列动作按钮均在视口内，页面水平溢出为 0。
- 减弱动态：浏览器媒体条件命中后，气泡与脚本反馈动画均为 `none`，核心状态和操作仍可用。
- 正式环境：`jma.mikeywa.site` 与 `jma-zeta.vercel.app` 均返回 200；线上 `index.html`、`src/lab.js`、`src/lab.css` 和抽样脚本音频与本地 SHA-256 一致。
- 正式浏览器回归：1440x1000 和 390x844 均无控制台或页面错误；动作 6 项、表情 5 项、脚本 5 套均可达，手机水平溢出为 0。
- 正式服务回归：`/api/health` 显示火山方舟与 Fish Audio 均已配置；`/api/director` 使用 Doubao Seed 2.0 Mini 真实返回儿童友好 JSON；`/api/tts` 返回有效星仔 MP3。

## 验收路径

1. 叫醒小墨点。
2. 任选预设能力，或输入“会把耳朵变成小雨伞”测试世界导演。
3. 选择帮助树洞朋友的方式并取名。
4. 等四步世界生长完成后进入花园。
5. 点击三个发光位置收集萤火种。
6. 走到雾门前，点击随能力变化的动作按钮。
7. 检查成长报告，并测试“收进我的图鉴”。

## 明确边界

当前正式第一关已经形成完整可玩闭环。新版 `/story-v2` 已接通豆包文本理解、豆包句末 ASR 校准和豆包动态 TTS；浏览器中间文本仍承担实时反馈，真正逐音频包返回的服务端双向流式字幕尚未替换这层混合方案。儿童账号、云端图鉴同步、家长端长期成长曲线仍不属于当前交付。

角色实验室当前是本机原型：兴趣档案没有云同步。问答塑形、连续脚本和场景选择完全走确定性本地规则，不消耗大模型额度。固定星仔台词优先使用本地缓存；自由台词会发送给 Fish Audio，项目服务端不保存，失败时仍保留文字和口型。

上线时需要在服务端配置 `ARK_API_KEY`、`FISH_AUDIO_API_KEY`，以及可选的模型变量。Seedream 5.0 Lite 4K 仅完成配置预留，正式产品入口、儿童内容安全和调用限额设计完成前不开放公网生成接口。

## 2026-08-25 新版故事模式基础

### 已完成

- [x] 输出《不见了的回声》《今天没有影子》《会走路的信》《倒着下的雨》四套适配同一交互骨架的故事发散。
- [x] 完成萌萌星“立体图鉴、声印、页灵、回声路”的世界规则，并推荐《不见了的回声》作为首版。
- [x] 新增独立 `/story-v2` 原型，不覆盖当前正式第一关和未提交的数据统计改动。
- [x] 完成随机图鉴员、4 轮自由回答、豆包偏好理解、确定性降级和宠物 recipe 生成。
- [x] 完成宠物签领、取名、三枚声印开口仪式和图鉴员/宠物不同音色。
- [x] 完成 3 章 6 场景、6 种 NPC 出场、18 个剧情选择、背包、道具详情、送出状态和结局回看。
- [x] 使用内置图像生成能力生成听风贝、星线团、没寄出的问候、灯塔种子 4 个透明水墨图标并压缩到 512px。
- [x] 新增 `/api/story-turn`，真实豆包请求返回儿童友好回应、偏好和受限宠物提示。
- [x] 实测 `/api/tts` 返回 44.1kHz 单声道 MP3；健康检查明确区分故事 AI、火山语音识别和宠物 TTS 供应方。
- [x] 在火山旧版豆包语音控制台创建 `kindergrimm_story_voice`（AppID `6570862201`），开通 ASR/TTS 2.0 与大模型兼容能力，读取 Access Token 并仅写入被 Git 忽略的服务端环境。
- [x] 配置 ASR 小时版 Resource ID `volc.bigasr.sauc.duration`、TTS Resource ID `volc.service_type.10029` 与音色 `zh_female_cancan_mars_bigtts`；`PET_TTS_PROVIDER` 已切到 `volc`。
- [x] 新增 Python 与 Vercel 两套 `/api/asr`，把浏览器 16kHz PCM 安全转给豆包，Token 不进入前端。
- [x] `/api/tts` 增加豆包/Fish 供应方切换与 `X-TTS-Provider` 验收标记。
- [x] Safari 交互改为浏览器中间文本即时反馈、豆包句末校准，并保留打字、点选和视觉降级。

### 当前验证

- Python、Vercel API 和浏览器模块语法检查通过。
- 本地 `/story-v2` 返回 200，4 个透明 PNG 均返回 `image/png`。
- 豆包真实回答已验证；输入“鲸鱼、星星、海、慢慢看”能抽取海洋主题、慢节奏和宠物提示。
- 豆包 TTS 真实返回 `200 audio/mpeg`；Python 路由返回 59,040 字节，Vercel 路由返回 72,480 字节，响应头均标记 `volc`。
- 豆包 TTS→ASR 回环在 Python 与 Vercel 两套接口都返回“你好，我们一起去找回声。”，证明鉴权、Resource ID、协议与音频格式已通。
- 本地健康检查为 `storyAi:true`、`speechRecognition:true`、`doubaoTts:true`、`petTtsProvider:volc`。
- Safari 已实测 `/story-v2` 首屏、豆包自动朗读、自由回答理解、反馈语音与进入第二问；未主动采集杨总现场麦克风音频。
- 360×800、390×844 与完整 6 场景回归仍未执行。

### 下一步

- [ ] 在浏览器中走完 4 轮访谈、宠物开口、6 场景和背包，并验证 360×800、390×844、1440×900。
- [ ] 若要做到完全由豆包“边说边出字”，把当前句末 `/api/asr` 校准升级为浏览器到服务端的双向流式中继；仍不能把 Access Token 下发到浏览器。
- [ ] 部署前在腾讯云环境变量写入同一组服务端配置，由 Nginx 暴露 `/api/asr`；当前只完成本机与 Vercel 函数级验证，未修改线上环境。
- [ ] 用真实儿童声线 A/B 当前豆包音色与 Fish Audio；试用包不足或要正式开通时必须单独确认计费。
- [ ] 杨总确认首选故事后，再决定是否让 `/story-v2` 替换正式故事入口、部署并发布。
