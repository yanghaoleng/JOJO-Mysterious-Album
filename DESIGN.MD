# 设计规范

## 体验基调

设计目标是“可以触摸的儿童绘本”，不是后台工具，也不是卡片堆叠的网站。画面需要同时具备手作温度、轻微神秘感和明确的游戏目标。

- 设计变化：6/10，保留 Kindergrimm 的纸张、水彩与石墨语言，只重排交互层级。
- 动效强度：6/10，角色和关键物持续有生命感，文本不过度飞入。
- 信息密度：故事模式 3/10，一屏只完成一个决定；实验室 5/10，同时保留预览、工具和解释。
- 固定明亮绘本主题，不随系统深色模式切换，避免破坏纸张美术方向。

## 色彩与材质

| 用途 | 变量 | 色值 |
| --- | --- | --- |
| 纸张底色 | `--paper` | `#f2ead9` |
| 深墨文字 | `--ink` | `#293028` |
| 次级文字 | `--muted` | `#697064` |
| 苔藓主色 | `--moss` | `#57704c` |
| 深苔藓 | `--moss-deep` | `#3f5639` |
| 暖金提示 | `--sun` | `#d1a44b` |
| 浅水蓝 | `--water` | `#8eada9` |

纸张使用低对比度噪点和纤维纹理。角色保持 Kindergrimm 的石墨边缘、水彩颗粒和轻微重绘抖动。界面按钮只使用苔藓绿作为主强调色，金色只用于萤火种、灯和胜利反馈。

## 排版与形状

- 标题使用 `Kaiti SC`、`STKaiti` 一类中文手写感字体。
- 正文使用系统无衬线字体，保证低龄儿童和家长都能快速阅读。
- 标题字重 700，正文行高不低于 1.65。
- 主按钮最小高度 52px，触屏关键目标不小于 48px。
- 圆角使用不对称有机比例，例如 `24px 18px 26px 20px`，避免统一胶囊感。
- 面板使用一层暖纸背景和细墨线，不叠加多层卡片。

## 动效参数

| 动效 | 时长 | 缓动/说明 |
| --- | --- | --- |
| 面板切换 | 320ms | `ease`，淡入和轻微上移 |
| 能力揭示 | 1450ms | 让孩子看清角色变化后再进入下一问 |
| 选择按压 | 120ms | 下沉 1px，缩放至 0.985 |
| 世界生长步骤 | 720ms/步 | 名字、萤火、雾和秘密门依次出现 |
| 角色移动 | 2.05 单位/秒 | 平稳、可中途改目标 |
| 目标涟漪 | 约 1.4 秒 | 点击草地立即反馈 |
| 萤火漂浮 | 连续 | 低频正弦运动，不抢夺注意力 |
| 头顶气泡入场 | 440ms | 轻微上弹，只表达角色开始说新一句话 |
| 气泡逐字显示 | 34ms/字 | Calligraph 平滑衔接字形，105ms 后开始 |
| 目光跟随 | 逐帧弹簧 | 刚度 105，阻尼按帧间隔衰减；离开预览后回正 |
| 脚本反馈入场 | 340ms | 只做 6px 上移和淡入；反馈语音结束后再换下一题 |

系统启用 `prefers-reduced-motion` 时，关闭非必要位移和装饰动画，保留状态变化。

## 场景规则

- 正交镜头呈现斜俯视箱庭，孩子不需要控制镜头。
- 主要目标在颜色、亮度和空间上都可辨识，不依赖纯文字。
- 角色始终处于可见区域，并有柔和地面阴影帮助判断位置。
- 第一关的路径有探索感，但不设置失败、倒计时或惩罚。
- 初始选择必须在关卡末尾产生可见作用，不能只出现在成长报告里。

## 引导与声音

- 引导员“小芽”每次只给一个动作目标。
- 所有语音均有同内容或等价内容的屏幕文字。
- 语音播放失败时保留同内容的气泡、口型与操作，不切换到设备机械 TTS。
- 按钮、收集、揭示和通关使用短促非语言音效，不持续循环背景音乐。
- 儿童选择后先反馈“你创造了什么”，再告诉下一步，避免评价对错。

## 模式选择与角色实验室

- 首次模式选择像摊开的图鉴扉页，只给“故事模式”和“角色实验室”两个明确入口。
- 实验室桌面采用“左侧工具、中间实时角色、右侧观察笔记”三栏结构；它是儿童角色工作台，不做后台面板风格。
- 问题始终出现在角色头顶气泡中，左侧同步显示答案与解释，做到“角色在问、孩子在回答”。
- 第一问决定高辨识度核心特征，后续问题只叠加身体比例和装饰，保证角色成长有因果且不会反复变脸。
- 所有实验选项即时重画；选中态只使用苔藓绿、细墨线和暖纸底，不引入第二套视觉语言。
- 实验室设计变化 6/10、动效强度 6/10、信息密度 5/10。

## 角色模板样本册

- 模板共 15 个，按“全部、小动物、人物”筛选，其中小动物 12 个，人物 3 个。
- 模板卡片必须展示真实 recipe 的程序化角色缩略图，不使用表情符号、文字占位或与套用结果不一致的插画。
- 两列样本册沿用暖纸、细墨线和轻微不对称圆角。套用态使用苔藓绿内框，仍可继续进入捏脸和身体面板修改。
- 动物模板先保证耳朵、眼睛、口鼻、尾巴和站姿的轮廓辨识；人物模板通过发型、眼镜、身体姿态和局部配色拉开差异。

## 角色清晰度

- 角色是“程序化 Canvas 部件纹理 + Three.js 平面骨骼”，不是浏览器原生 SVG；可复现不等于无限分辨率。
- 角色实验室和第一关的部件绘制密度固定为 `U = 176`，比原调试值 88 提高一倍；纹理使用线性缩放、mipmap 与 8 倍各向异性采样。
- WebGL 画布显示像素比为 `clamp(1.5, devicePixelRatio, 2.5)`，普通屏也做 1.5 倍超采样，Retina 屏不超过 2.5 倍以控制移动端显存。
- 清晰化只提高采样密度，不消除纸张颗粒、石墨毛边和两帧轻微重绘；这些是绘本风格的一部分，不应被误当成模糊滤镜删除。

## 儿童画像节奏

- 画像分为基本认识、探索习惯、表达与伙伴、兴趣地图、陪伴方式、最近着迷 6 个阶段，共 17 问；一屏只出现一个问题。
- 每次选择后保留当前选中态，先给一句不评判对错的反馈，语音结束后再进入下一问；进度同时显示阶段、题号和“已记住”数量。
- 右侧笔记按 6 个主题分组摘要，不把 17 个字段堆成成人问卷；手机端仍把角色和答题区放在前面。
- 不询问姓名、学校、住址、联系方式或精确生日。年龄只选择区间，敏感偏好只用于降低声音、惊吓、倒计时与竞争压力。
- 第一关必须消费画像：开场使用主题与玩法，难点使用受挫和鼓励方式，结尾使用角色称号与故事感觉；没有画像时使用安全的通用文案。

## 角色头顶气泡

- 气泡使用双层纸边、内侧浅苔藓线、轻微折痕和纸尾巴，避免厚描边与普通白色卡片感。
- 气泡锚点来自角色实时包围盒的头顶位置，并投影到预览区坐标；角色换比例、换场景或漂浮时都要跟随。
- 纸尾巴固定在气泡水平中心，气泡整体放在角色头顶之上，并在预览边缘内自动收束。
- 每次内容变化都重新执行 440ms 入场；文字由 Calligraph 接收逐步增长的字符串，实现逐字出现与已有字形的平滑过渡。
- 气泡只动画 `transform` 和 `opacity`。启用 `prefers-reduced-motion` 时取消入场并一次显示完整文字。
- 屏幕阅读器只读取完整句子，不逐字重复播报。

## 角色注视与点击

- 角色眼睛和头部共同跟随预览区内的鼠标或触摸位置，眼睛先到、头部随后轻微超调，不使用生硬的瞬时切换。
- 指针靠近底部按钮时角色自然低头注视按钮；触屏按下后短暂保持目光，再自动回正。
- 只有射线命中角色本体时才触发点击反馈。反馈由一句短文字、一次轻动作和对应的星仔 Fish Audio 缓存组成，850ms 内不重复触发。
- 开启 `prefers-reduced-motion` 时禁用指针目光位移，保留按钮和点击反馈的文字语义。

## 角色场景册

- 22 个场景分成“日常、自然、奇遇”，用两列水彩缩略舞台承载，避免把长名单做成后台式下拉框。
- 缩略图与实时场景使用同一套程序化绘制函数，选到的舞台和预览不会出现视觉货不对板。
- 场景不是纯背景换色：每个场景定义角色比例、落脚高度和动作类型。书桌上的角色会缩小，宇宙取消地面并慢慢失重漂浮，云朵与海底使用较轻的上下漂移。
- 宇宙、月亮等深色环境只存在于角色预览画框内，实验室 UI 仍锁定明亮纸张主题。
- 漂浮、风动、火车颠簸等循环动作只使用根节点位移和旋转；开启 `prefers-reduced-motion` 后角色保持静止。
- 场景选中态沿用苔藓绿和有机圆角，缩略图承担主要识别，文字只说明环境和动作差异。

## 实验室语音策略

- 星仔是首次进入和重新开始后的默认音色，声音页明确标记“默认试听”。
- 默认星仔的问答、反馈、模板、场景和动作固定语句优先使用项目内置的 Fish Audio MP3；切换其他音色或自由台词时调用在线 Fish Audio。
- 在线与缓存都不可用时保留气泡和口型，并给出轻量状态提示；故事和实验室都不使用设备系统 TTS，避免突然出现机械声线。
- 音色试听始终播放随项目打包的四段固定样本，保证产品调试时可快速对比。

## 动作与连续互动

- 挥手和表情模拟统一归入“动作”标签，不在角色预览底部叠加常驻按钮；预览只保留一个低强调度的“换”字随机印章。
- 动作预览提供挥手、行走、奔跑、坐下、休息和勇气动作，按钮激活期间保持苔藓绿选中反馈，动作结束后回到待机。
- 连续脚本每题只允许选择一次。孩子选择后，原选项仍留在当前题里并标明选中，其他选项弱化；反馈句和对应动作完成后才进入下一题。
- 已选答案以短标签持续留在脚本记录中。三题结束后生成一句观察摘要，并写入右侧本机档案。
- 5 套脚本覆盖森林探索、雨天安静活动、帮助迷路星星、制作朋友礼物和睡前故事，内容用于兴趣与偏好收集，不做能力高低判断。

## 响应式

- 桌面：场景铺满画面，故事面板靠左，角色从面板右侧露出。
- 手机竖屏：面板置于下半屏，场景和角色保留在上半屏，选项单列。
- 手机横屏：压缩标题、边距和面板高度，保证主操作不被底部安全区遮挡。
- 角色实验室竖屏：角色预览占上方 43%，工具区占下方 57% 并独立滚动；切换按钮放在标题右上角。
- 角色实验室横屏：角色预览在左侧 44%，工具区在右侧 56%，保留完整标签和可滚动答案。
- 使用 `100dvh` 和安全区变量，兼容移动浏览器地址栏和圆角屏幕。

## 访问数据后台

- `/Data` 是轻量项目运营页，不伪装成儿童玩法；仍沿用暖纸、石墨线、苔藓绿、不对称有机圆角和轻颗粒，数据层级优先于装饰。
- 设计变化 4/10、动效强度 2/10、信息密度 7/10。核心数据用四张概览卡，页面、深度、事件和每日趋势各用一个明确面板，不加入无意义插图或渐变。
- 口令门复用罗师傅档期页体验：六个底线圆点、三列数字键盘、`1 至 9 / 清除 / 0 / 退格` 顺序、六位后 80ms 自动提交；错误只使用一句提示和 320ms 横向抖动。
- 图表只使用苔藓绿和暖金区分 UV/PV。所有数值同时有文字、表格或标题，不只依赖颜色；空数据、读取失败和重新读取都有独立状态。
- 手机使用两列核心指标、单列数据面板和可横向查看的页面表格；触屏按钮不小于 42px，键盘按钮使用 4:3 比例。
- 后台不显示或推断孩子输入、姓名、声音、IP、精确位置与个人兴趣原文；隐私说明固定放在页面底部。

## 首次加载策略

- 模式扉页只加载 `index.html`、实验室基础 CSS、模式逻辑和匿名统计，压缩后约 22KB；不在孩子选择前下载 Three.js、角色骨骼、Calligraph 和另一套模式。
- 选择故事后按需加载故事图形模块，选择实验室后按需加载实验室、Calligraph 和角色图形模块。离线语音和音乐继续按播放进度单独请求，不打进入口包。
- JavaScript 和 CSS 短缓存一小时，图片与 MP3 缓存七天；HTML、API 和 `/Data` 不缓存。发布保留版本目录与旧 Vercel 回滚点。

## 新版连续故事

- 设计读取：面向 5 至 8 岁孩子的可触摸水墨绘本，不做聊天工具或任务列表。设计变化 6/10、动效强度 6/10、故事信息密度 3/10。
- 一屏只保留一个人物问题和一组当前选择。章节、场景和背包是长期状态，不能同时堆进中央面板。
- 宠物在画面左侧持续可见，当前 NPC 在右侧出场，纸面板承担对话与选择；手机端宠物退到低对比背景，但不完全消失。
- NPC 出场动效必须说明来源：门是横向探出、草堆是向上冒、箱子是压扁弹起、倒影是上下翻转、帘子是侧向滑出、灯罩是缩小落下。
- 场景转换使用 760ms 墨洗遮罩，只在跨场景时出现；所有持续角色动效只改变 `transform` 和贴图帧。
- 背包固定在安全区右下角。得到道具时只跳动一次，数量显示未使用物品；已送出的道具仍保留来历和说明，但降低不透明度。
- 道具图标统一为 512×512 透明 PNG，水彩晕染、石墨抖线、单物件居中，不带文字、边框和投影底板；64px 下仍需能辨认轮廓。
- 语音输入、打字和点选是等价入口。语音不可用时直接说明并把焦点交给输入框，不制造失败动画。
- 语音按钮第一次按下开始听，第二次按下收好这句话；Safari 可用时中间文本即时出现，句末状态依次为“豆包正在确认”与“豆包听见了”。
- 浏览器中间识别只负责低延迟反馈，豆包服务端识别负责最终文本；两者冲突时以豆包结果替换输入框，但仍允许孩子手动修改。
- 豆包理解期间显示具体状态“正在理解刚才说的内容”，不用无意义转圈。远程理解失败时保留回答并执行确定性宠物映射。
- 开口仪式固定三枚声印：名字、听见、声音。宠物开口前使用动作和省略式气泡，开口后才播放与图鉴员不同的音色。
- 宠物 TTS 必须保持与图鉴员可辨认的独立听感；当前豆包音色用 `star/sprout/bubble/moss` 四档语速塑造节奏差异。远端合成不可用时只保留气泡、口型和动作，不突然切换设备系统声线。
